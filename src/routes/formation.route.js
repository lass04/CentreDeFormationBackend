import { Router } from "express";

const router = new Router();

//router.route('/getFormations').get();
//router.route('/updateFormation').patch();
//router.route('/deleteFormation').delete();
//router.route('/createFormation').post();

export default router;