import { Router } from "express";

const router = new Router();

//router.route('/getSessions').get();
//router.route('/updateSession').patch();
//router.route('/deleteSession').delete();
//router.route('/createSession').post();

export default router;