import { Router } from "express";

const router = new Router();

//router.route('/getFormateurs').get();
//router.route('/updateFormateur').patch();
//router.route('/deleteFormateur').delete();
//router.route('/createFormateur').post();

export default router;